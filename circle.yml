machine:
  node:
    version:
      0.10.28
test:
  pre:
    - npm start:
        background: true
    - sleep 5
  override:
    - sudo apt-get install npm
    - npm run test-single-run
    - npm run protractor
